[project]
name = "embedded-debug"
version = "3.0.0"
description = "Serial monitor and debug tools for Rockchip embedded targets — socat-based with multi-agent support"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
target-version = "py310"
