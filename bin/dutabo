#!/bin/bash
# Bootstrap shim for dutabo serial console CLI.
# Downloads the prebuilt binary from GitHub Release on first use.

SCRIPT_DIR="$(cd -- "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/../scripts/ensure-binary.sh" "dutabo" "$@"
