[project]
name = "embedded-debug-mcp"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "pyserial>=3.5",
    "pexpect>=4.9",
]

[tool.ruff]
line-length = 100

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
